# Supabase CLI local cache — records which project this checkout is linked to
# plus version stamps and a pooler URL. Regenerated by `supabase link`, and
# machine-specific, so it should not be shared between computers.
supabase/.temp/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
